/**
 * Regras de domínio puras: policy engine, invariantes de etapa e motor de
 * distribuição. Sem I/O — tudo aqui deve ser testável sem banco nem rede.
 *
 * Preenchido na Fase 1 (`PLANO.md` §4).
 */
export {};
