/**
 * Contratos de integração: `MessagingProvider` e `ExternalCrmAdapter`, com
 * implementações mock e stubs que lançam `NotImplementedError`.
 *
 * Estrutura pré-pronta desde a Fase 1; provedores reais só na Fase 4
 * (`ARCHITECTURE.md` §5).
 */
export {};
